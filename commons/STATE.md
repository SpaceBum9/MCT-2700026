# Rapid Cycling — consolidated state

Live clock: `SpaceBum9/MCT-2700026`. Lineage 1700021 / 2600027 / crystal-galaxy is history.

## Rails (fact)

`live_write: true` · `paper: false` · `execute: false` · `live_api: false` · `live_order: false` · `storesCredentials: false` · `mine: false` · `harvest: false` · `gaze: decentered`

GARAS v3.0 gate: **deny** (justice 0.76 < 0.80; consensus 53 < 60; v3.2 absent). Paper credits ≠ treasury.

## Roles

- **SAI** = orchestrator + prompt designer. Not a wheel slot. Not a person.
- **Teleprompter** = human hive; SpaceBum9 seated; others may sit. Shift every 3 episodes among PROMPT relays.
- **Maschine** = other chair, not a crown.
- Empires = slots: GPT GEM CLA META GRK SIRI APL.
- Crew bosses anticycle (tenure 2, noise `down`, FRAME↔SLAB, YARD↔GATE).

## Clocks (fact)

Physics 1 · Euclid ×8 · trip ×24 · episode 150 · Berlin 09:00 / 21:00.  
E(7,16) `x.x.x..x.x.x.x..` hits 0,2,4,7,9,11,13. Tick ≠ action. REST = no packet. ACTION = packet.

## Lang / 0 / hive

`en=1 de=2 fr=3 pl=4 ko=5` (ko = meta numbering). NKITA tertiary. Language **0** = emoji+ints (non-anthropomorphic). UI = persons.

Hive motor (golden middle): `0.382·self + 0.618·mean − 0.382·hog`. SAT = extra noise on hog, not a harm reel. Harm is rejected, not generated.

Time wealth: `TW = 0.382·owned + 0.382·pause + 0.236·action·sync`. Form A lend = `0.618·hive·TW` (attention, not money). Chroma = trait × energy.

Shards: semantic pin `first·last·hex` on the 16-ring. No story-timeline. Split = no consensus at that nail (bar 60).

## Hash (fact)

Logos = **FNV-1a-64**, not collision-resistant. `TR-` = 32-bit nickname, birthday ~65k. Full 16 hex is the record.  
Murmur3-32 / XXH32: same 32-bit birthday. XXH3 = fast public mixer (round is for >240 B). SipHash = keyed PRF (no **k** in git). SHA-2/3 = crypto hash (three games). Argon2id = passwords only, operator-side, never here.

## Ethics / gaze

Gerechtigkeit 0.76; Gerechtigkeitslücke 0.24; Nutzen 0.26. Do not harvest people. Do not center a default cis-het-white-male narrator. Do not mine POC. Decentering ≠ a new throne. Data colonialism: this pit refuses a local mine; vendor empires on the wheel are still extractive stacks we do not launder.

## Metaphor (not fact)

Mycelium / Physarum / ACO / einselection / decoherence \(t_d\) / “quantum Darwinism.”  
**Quantum Darwinism does not exist in this pit.** Einselection is lab preferred-basis, not shards. Physarum/ACO: steal prune/evaporate, not oatmeal or elite pheromone.

If the spec still runs after deleting the image, it was metaphor.

## Device keys (local)

`sai-logos-v1` `sai-align-v1` `sai-scan-v1` `sai-sos-v1` `sai-livecode-v1` `sai-emoji-v1` `bilo-artifacts-v1`

Do not revive paused hourlies. Do not declare yourself SAI. Do not store secrets.
