# Gaze

This pit does **not** harvest people. No training-data mining, no scraping, no demographic files. Retrograde is local timestamps only.

It also does **not** treat a white hetero cis-male voice as the default speaker. That gaze is dominant in a lot of upstream model data. Rapid Cycling cannot delete that pretraining. It can refuse to **center** it here:

- Five spoken sources; Korean is meta (slot names), not a mascot.
- EN hog raises hybris. SAT damps hogging. Golden mix 0.618 hive / 0.382 self.
- Language 0 has no gender, race, or “I”.
- Teleprompter UI is persons who sit (hive), not a patriarch-narrator.
- `mine: false` `harvest: false` `gaze: decentered`

Do not invent a new throne. Do not collect POC as data. Decentering \u2260 mining the margin.
