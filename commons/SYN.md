# Synaesthetic multimodal decode

One goal: **bind** rails that already exist. Not a clinical synaesthete. Not a new mind.

On ACTION, one **chord**:

`{sense} {lang} h{hue} m{morph} {pin}`

- sense ← empire (voice/pic/video/freq)
- lang ← wave (`en…ko gs brl`)
- hue ← time-wealth chroma (trait × energy)
- morph ← Hangul blocks or Braille cells or tokens
- pin ← semantic shard on the Euclidean nail

Decode = that bind. Gesture gloss and Braille are channels in `lang`, not extra cameras. Language 0 can carry the chord. UI still speaks in persons.

No harvest. No extra timeline. `execute: false`.
