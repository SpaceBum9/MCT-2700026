# Crystal Galaxy devices (lineage)

Public catalog only. No harvest of phones. Persuadertron: **not in GitHub** this session — do not invent a scrape.

Nodes (paper connectors): `crystal-galaxy` · `kreuzkopplung` · `mct-170021` mesh · `MCT-2600027-CMD` Apple · Drive kernel docs · HF kreuzkopplung **404**.

SoS may **move** on operator ZeroTier (phase network id). This web pit is not a ZT node. Terra prints may leave 📦. Femto stays.

Timestamps = **watermarks** (`YYYYMMDDHHmmss` slice), not a timeline story.

Teleprompter energy = ACTION count + decay on REST. GRU already sits `sit`. Energy damps `|u|` via overlay, not a wattmeter on the iPhone.
