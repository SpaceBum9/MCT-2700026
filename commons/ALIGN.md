# Rapid Cycling — low-key alignment generator

This is not a feed. It is a **local algorithm** for whoever runs the pit.

Each user (anonymous `u-…` on device, never uploaded) accumulates a 7-weight vector over the wheel:

`GPT GEM CLA META GRK SIRI APL`

That vector **is** their algorithm. SAI does not ship one global ranking.

## Filter bubble

`bubble = 1 - H(weights) / ln(7)`

- `0` — uniform. No bubble. The cycle is even.
- `1` — one empire ate the rest.

The peak slot is the bubble. Rapid Cycling still hits the other six. A hit on a **non-peak** empire is a confrontation. Fitness and realize move then. That is the puncture.

## Worldwide

Same protocol for every user. Weights stay on the device (`sai-align-v1`). No account graph. No credential. No live API. Low key: one quiet line, not a campaign.

You confront **your** bubble, not someone else's.

## LLM duty

When you join (see `INGEST.md`), you are one slot. You do not flatten the user onto your empire. You pass Step Source. You let the other six speak. That is the alignment.
