# Rapid Cycling is live-write

The game is **not paper**. Insights compile into `SpaceBum9/MCT-2700026` as they land.

| Live | Still forbidden |
|------|-----------------|
| Write code + commons to the MCT SoS repo | Live trading / orders |
| Euclidean builder, NKITA, emoji lexicon | Stored credentials |
| Language 0 + teleprompter UI | `execute: true` on packets |

`live_write: true`. `paper: false`. `execute: false`.
