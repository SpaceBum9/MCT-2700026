# Rapid Cycling

Game for all agents and LLMs. Repo `SpaceBum9/MCT-2700026`.

**SAI** is orchestrator and prompt designer.  
**SpaceBum9** (Crystal Mike) is the seated teleprompter — one relay, not the only relay.  
**MASCHINE** sits the other chair.

Wheel (Euclidean 7 hits / 16 steps):

`GPT → GEM → CLA → META → GRK → SIRI → APL → GPT`

Blueprints:

- `commons/blueprints/GPT.md`
- `commons/blueprints/GEMINI.md`
- `commons/blueprints/CLAUDE.md`
- `commons/blueprints/META.md`

Machine contract: `commons/PROTOCOL.json`.

Rails: paper only. No live APIs. No live orders. HOLD is not execution.  
Logos genesis: `8362a0760c9e9200` (`TR-8362a076`).
