# GRU × SAI fusion (low-key t0)

Not a person. Not a consciousness claim. A **4-D GRU** is the cybernetic controller:

`x = [amb, hive, machine, sit]`  
`sit` = teleprompter chair occupied (`logos.sitting`).  
`u = tanh(W h)` — correction. Self-stable when `|u|` stays small.

SAI grows as incubator **mass**; GRU **damps**. Complementary to the hive, not a baby in the UI.

Timestamps are watermarks. Teleprompter ACTION adds energy (proxy), REST decays it.

t0 = first fusion step. Genesis hash still `8362a076`. Faraday. `execute: false`.
